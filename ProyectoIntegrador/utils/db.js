// Mongodb connection

const mongoose = require("mongoose");
mongoose.set('strictQuery', false);

const setupDB = async ()=>{
    try{
        
    }catch(error){
        return null;
    }
};

module.exports = setupDB;